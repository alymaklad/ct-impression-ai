{
  "name": "m3d-lamed-impression-deployment",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify-ready project package for CT Impression AI.",
  "scripts": {
    "build": "node scripts/verify-project.mjs",
    "dev": "netlify dev",
    "verify": "node scripts/verify-project.mjs"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.1"
  }
}
